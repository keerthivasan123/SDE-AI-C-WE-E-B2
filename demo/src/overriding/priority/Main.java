package overriding.priority;

import overriding.priority.Parent;
import overriding.priority.Child;

public class Main {
    static void main() {
        Child c = new Child();
        c.show();
    }
}
